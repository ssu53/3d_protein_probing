name: 3d_protein_probing

channels:
  - conda-forge
  - pytorch
  - nvidia

dependencies:
  - python=3.10
  - biotite
  - cudatoolkit=11.3
  - einops
  - ipdb  # for debugging
  - ipython  # for debugging
  - matplotlib
  - numpy
  - pandas
  - pip
  - pytorch
  - pytorch-lightning
  - scikit-learn
  - scipy
  - tqdm
  - wandb
  - pip:
    - e3nn
    - fair-esm
    - tensorboardX
#    - torchdrug  # requires python 3.9
    - typed-argument-parser>=1.8.0
